<templates id="template" xml:space="preserve">
    <!--
    Odoo 19 restructured the ClosePosPopup template completely.
    It now uses <Dialog> and <PaymentMethodBreakdown> components.
    The previous xpath (//div/main/div) no longer exists.
    
    The original override added a scroll container to payment methods.
    This would need to be reimplemented for the new layout.
    -->
</templates>
